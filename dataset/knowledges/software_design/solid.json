[
  {"text": "SOLID is a set of five principles for object-oriented and software design that make systems easier to maintain, extend, and understand. Each principle addresses a specific type of design problem that causes code to become rigid, fragile, or hard to change over time."},
  {"text": "Single Responsibility Principle (SRP): A module, class, or function should have only one reason to change. This means it should do one thing and do it well. When a component handles multiple concerns, a change in one concern forces changes in unrelated parts — increasing risk and complexity."},
  {"text": "Open/Closed Principle (OCP): Software entities should be open for extension but closed for modification. New behavior should be added by extending existing code, not by changing it. This protects stable, tested code from being broken by new requirements."},
  {"text": "Liskov Substitution Principle (LSP): Objects of a subtype should be replaceable with objects of their parent type without breaking the program. If a function works with a base type, it must work correctly with any derived type. Violating LSP leads to unexpected behavior and fragile inheritance hierarchies."},
  {"text": "Interface Segregation Principle (ISP): Clients should not be forced to depend on interfaces they do not use. Large interfaces should be split into smaller, focused ones. This prevents components from being coupled to behavior they do not need, reducing the impact of changes."},
  {"text": "Dependency Inversion Principle (DIP): High-level modules should not depend on low-level modules. Both should depend on abstractions. Abstractions should not depend on details — details should depend on abstractions. This decouples components and makes systems easier to test and change."},
  {"text": "SOLID principles in Go differ from classical OOP languages. Go uses interfaces implicitly — a type satisfies an interface by implementing its methods without declaring it. This naturally supports ISP and DIP. Go favors composition over inheritance, making LSP less relevant but SRP and OCP still critical for clean package design."}
]
